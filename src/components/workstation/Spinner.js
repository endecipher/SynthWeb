import React from 'react';
import PropTypes from 'prop-types';

const Spinner = ({
    
}) => {
    return (
        <div className="loader">
            Compiling..
        </div>
    )
}

Spinner.propTypes = {

}

export default Spinner;
