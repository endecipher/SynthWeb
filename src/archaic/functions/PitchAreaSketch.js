export default function pitchareasketch(){

}
/*
export default function pitchareasketch(p){
p.x=100;
p.y=100;

p.setup=function() {
  p.x=p.width/2;
  p.y=p.height/2;
  p.noStroke();
  p.background(51);
}


p.myCustomRedrawAccordingToNewPropsHandler=function(newProps){
  if(newProps.handlePitchChange){
    p.sendXY=p.handlePitchChange;
  }
}

p.draw=function(){
  p.fill(255,0,200,25);
  p.noStroke();
  p.ellipse(p.x,p.y,48,48);
  p.sendXY(p.mouseX,p.mouseY)
  // p.x=p.x+p.random(-15,15);
  // p.y=p.y+p.random(-15,15);
}  

}//sketch for pitch area ends
*/